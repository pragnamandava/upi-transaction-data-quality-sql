# upi-transaction-data-quality-sql
SQL-based data quality and cleaning pipeline for UPI transaction data, handling invalid formats, duplicates, and business rule violations using CTEs, regex, and window functions.
